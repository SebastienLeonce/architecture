version: "3"
services:
  cadvisor:
    image: "google/cadvisor:latest"
    container_name: cadvisor
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
      - /dev/disk:/dev/disk/:ro
    devices:
      - /dev/kmsg:/dev/kmsg
    labels:
      traefik.http.routers.cadvisor-architecture.rule: Host(`cadvisor.localhost`)
    networks:
      - local
    ports:
      - "8081:8080"
    restart: always
